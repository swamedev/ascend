"""Version constants for the ASCEND platform."""

RUNTIME_VERSION = "0.1.0"
SDK_VERSION = "0.1.0"
CONTRACTS_VERSION = "0.1.0"
ERRORS_VERSION = "0.1.0"
API_VERSION = "1.0.0"

VERSIONS = {
    "runtime": RUNTIME_VERSION,
    "sdk": SDK_VERSION,
    "contracts": CONTRACTS_VERSION,
    "errors": ERRORS_VERSION,
    "api": API_VERSION,
}
